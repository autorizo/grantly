import { {{pascalCase iconname}}Props } from './{{pascalCase iconname}}.types';

export const {{pascalCase iconname}} = ({ className }: {{pascalCase iconname}}Props) => (
  <svg viewBox='0 0 24 24' fill='none' width='1em' height='1em' className={className}>
    <path d='' stroke='currentColor' />
  </svg>
);
